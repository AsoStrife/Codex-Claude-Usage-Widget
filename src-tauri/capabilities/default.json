{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Permissions required by the tray popup window. Without this file Tauri denies every core-plugin command, which silently breaks `event.listen()` in the webview.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:allow-start-dragging",
    "autostart:default"
  ]
}
